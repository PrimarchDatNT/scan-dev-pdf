<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_container_action_page"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/color_white"
    android:orientation="vertical"
    app:layout_constraintBottom_toBottomOf="parent"
    tools:context=".features.detailshow.detailpage.DetailPageActivity">

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/color_line" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal"
        android:weightSum="10">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_select"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickRotate"
            android:padding="16dp"
            android:src="@drawable/detail_page_vector_rotate" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_camera"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickCrop"
            android:padding="15dp"
            android:src="@drawable/enhance_vector_crop" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_import"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickDeleteItem"
            android:padding="13dp"
            android:src="@drawable/detail_page_vector_delete_black" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_share"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickShareItem"
            android:padding="16dp"
            android:src="@drawable/all_vector_sendto_share_black" />

    </LinearLayout>

</LinearLayout>
