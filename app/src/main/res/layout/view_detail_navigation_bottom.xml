<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cl_container_action_detail"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/color_background"
    android:orientation="vertical"
    app:layout_constraintBottom_toBottomOf="parent">

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/color_line" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal"
        android:weightSum="10">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_select"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickSelect"
            android:padding="16dp"
            android:src="@drawable/all_vector_select" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_camera"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickCamera"
            android:padding="15dp"
            android:src="@drawable/detail_status_selector_camera_press" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_import"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickImportImage"
            android:padding="15dp"
            android:src="@drawable/detail_status_selector_import_press" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_share"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@null"
            android:onClick="onClickShareDoc"
            android:padding="16dp"
            android:src="@drawable/all_vector_sendto_share" />

    </LinearLayout>

</LinearLayout>
